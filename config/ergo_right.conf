# RIGHT half - now a PERIPHERAL (the dongle is the central).
#
# Nothing central-only goes here anymore. CONFIG_ZMK_STUDIO was removed: Studio
# now runs on the DONGLE (the central). Leaving Studio enabled on this
# peripheral bloats its build and interferes with it connecting as a
# peripheral - the likely reason the right half went unresponsive after the
# dongle switch.
#
# (Trackpad relay config will be added here in the trackpad phase.)
